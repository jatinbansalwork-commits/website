{
  "name": "landing-template",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5175",
    "check:html": "python3 scripts/check-html-shell.py",
    "build": "npm run check:html && node scripts/vercel-static-build.mjs",
    "build:vite": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
